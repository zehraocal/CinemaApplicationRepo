import { MovieTicketGetSession } from './movie-ticket-get-session';

describe('MovieTicketGetSession', () => {
  it('should create an instance', () => {
    expect(new MovieTicketGetSession()).toBeTruthy();
  });
});
