import { VisionMovieUpdateVM } from './vision-movie-update-vm';

describe('VisionMovieUpdateVM', () => {
  it('should create an instance', () => {
    expect(new VisionMovieUpdateVM()).toBeTruthy();
  });
});
